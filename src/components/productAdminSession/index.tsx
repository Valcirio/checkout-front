import { TRegisterProduct } from '@/validators/product'

export default function ProductAdminSession({ product }: { product: TRegisterProduct }) {
	return (
		<section className="flex flex-row items-start justify-center">
			<article></article>
			<article></article>
		</section>
	)
}
